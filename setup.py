from setuptools import setup, find_packages

packages = find_packages(exclude=['tests*'])

setup(
    name='diva',
    version='0.1',
    packages=packages,
    url='',
    license='MIT',
    author='John Henning',
    author_email='',
    description=''
)
